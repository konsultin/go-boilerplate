core:
  route:
    - get: /
      handler: HealthCheck
    - post: /v1/cron/{cronType}
      handler: HandleCronTrigger
    - post: /v1/users/anon/sessions
      handler: HandleCreateAnonymousUserSession
    - put: /v1/users/sessions
      handler: HandleUserRefreshToken